# Exclude model artifacts from image (add if not needed in container)
src/models/*.json
# 🐍 Python virtual environments and bytecode
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.Python/

# 📦 Dependency & lock files (handled by uv inside Docker)
uv.lock
# Only ignore `pyproject.toml` if you use `requirements.txt` instead.
# If your Dockerfile installs with `uv pip install -e .`, DO NOT ignore pyproject.toml.
# pyproject.toml

# 🧠 AI/ML artifacts (optional: if these aren't needed inside image)
*.pkl
*.joblib
*.h5
*.pt
*.onnx

# 🧪 Test, notebooks, debug
tests/
*.ipynb
notebooks/

# 🧰 IDE/editor settings
.vscode/
.idea/

# 🌳 Git
.git/
.gitignore

# 📁 Logs and temp files
*.log
logs/
tmp/
__tmp__/
.DS_Store

# 🔒 Sensitive credentials
.env
.env.*

# 🏗 Build artifacts
dist/
build/
site/

# 🚫 CI/CD, Docker tooling (optional exclusions)
docker-compose.yml
docker-compose.override.yml
Dockerfile.dev
